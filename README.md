# محرك عروض الشعر النبطي

محرك مستقلّ لتحليل ووزن الشعر النبطي: يأخذ بيتًا فيعطي البحر الأقرب،
ودرجة المطابقة، والتفعيلات، والتقطيع، والنمط الرقمي، وموضع الكسر،
والبدائل، وتفسير النتيجة.

**حتمي بالكامل.** لا نموذج لغوي في قرار الوزن، ولا عشوائية، ولا نسبة
مقدَّرة. النصّ نفسه مع البيانات نفسها يعطي النتيجة نفسها دائمًا.

---

## جرّبه الآن — من أي متصفح

```
https://faisaltlal.github.io/-/web/
```

يعمل كاملًا داخل متصفحك: لا خادم، ولا شبكة، ولا يُرسَل نصّك إلى أي
مكان. يعمل على الآيفون والتابلت بلا تثبيت شيء.

### ملف واحد مكتفٍ بذاته

`web/standalone.html` — المحرك كله (١٢٩ كيلوبايت) في ملف HTML واحد:
لا استيراد وحدات، ولا ملفات جانبية، ولا إنترنت. حمّله وافتحه من أي
مكان، أو مرّره عبر أي خدمة معاينة. مفيد قبل أن يصير للمشروع موقع
منشور، ومجرَّب فعليًا من `file://` بلا خادم.

يُولَّد من ملفات المحرك نفسها بـ `node tools/build-standalone.js`،
وCI يفشل إن تأخّر عنها — فلا يمكن أن ينحرف.

📱 **تفاصيل العمل من تابلت وآيفون:** [`docs/RUNNING-FROM-A-TABLET.md`](docs/RUNNING-FROM-A-TABLET.md)

---

## التشغيل محليًا

```bash
node tests/run.js            # الاختبارات
node tools/bundle-data.js    # إعادة توليد نسخ البيانات بعد تعديل data/
DEBUG_PROSODY=true node …           # وضع التتبّع
```

```bash
cd swift && swift test       # حزمة Swift (تحتاج toolchain)
```

بلا اعتماديات: Node ١٨+ وحده يكفي لكل شيء عدا Swift.

---

## الاستعمال

```js
import { createEngine } from './engine/js/index.js';
import { DATA } from './engine/js/data.generated.js';

const engine = createEngine(DATA);

const r = engine.analyze('مُسْتَفْعِلُنْ مُسْتَفْعِلُنْ فَاعِلَاتُنْ');
r.bestMeter.name       // 'المسحوب'
r.bestMeter.score      // 1
r.verdict              // 'sound'
r.internalPattern      // 'LLSLLLSLLSLL'
r.tafaeel              // كل تفعيلة مع صورتها وكلماتها
r.brokenFeet           // موضع الكسر إن وُجد
r.alternatives         // البحور البديلة مرتَّبة

engine.analyzePoem(text);              // البحر الغالب والتوزيع والأبيات الخارجة
engine.train(bayt, 'المسحوب');          // وضع التدريب
engine.listMeters();                   // القاعدة كاملة
engine.openQuestions();                // كل ما لم يُحسم
```

```swift
import NabatiProsody

let engine = try ProsodyEngine()
let r = engine.analyze("مُسْتَفْعِلُنْ مُسْتَفْعِلُنْ فَاعِلَاتُنْ")
r.bestMeter?.name   // "المسحوب"
```

---

## البنية

```
.
├── data/            ← المصدر الوحيد للحقيقة
│   ├── meters.json          البحور
│   ├── tafaeel.json         التفعيلات
│   ├── variations.json      الزحافات والعلل
│   ├── scoring.json         كل معاملات الدرجة
│   ├── encodings.json       الترميزات الرقمية
│   ├── lexicon.json         الاستثناءات الصوتية
│   └── golden_cases.json    الحالات المرجعية
├── engine/js/       التنفيذ المرجعي
├── swift/           حزمة Swift (لتطبيق iOS لاحقًا)
├── web/             واجهة التجربة في المتصفح
├── tests/           الاختبارات
├── tools/           مولّد نسخ البيانات
└── docs/            التوثيق
```

**لا يوجد في كود المحرك اسم بحر واحد، ولا نمط واحد، ولا رقم درجة
واحد.** إضافة بحر تعديلٌ في `meters.json` وحده.

---

## التوثيق

| الملف | الموضوع |
|---|---|
| [`ARCHITECTURE.md`](docs/ARCHITECTURE.md) | البنية والقراران الهندسيان الكبيران |
| [`PROSODY.md`](docs/PROSODY.md) | قواعد التطبيع والتقطيع كلها |
| [`METERS.md`](docs/METERS.md) | قاعدة الأوزان والفروق عن الخليل |
| [`SCORING.md`](docs/SCORING.md) | معادلة الدرجة كاملةً |
| [`ADDING-A-METER.md`](docs/ADDING-A-METER.md) | **كيف تضيف بحرًا بنفسك** |
| [`OPEN-QUESTIONS.md`](docs/OPEN-QUESTIONS.md) | **ما لم يُحسم وما ينقص لحسمه** |
| [`RUNNING-FROM-A-TABLET.md`](docs/RUNNING-FROM-A-TABLET.md) | العمل من تابلت أو آيفون |

---

## حدّ الدقة — اقرأ هذا

**دقّة المحرك مقيَّدة بتشكيل ما تُدخله.**

| المُدخل | التقطيع | النتيجة |
|---|---|---|
| مشكول | قاطع — قراءة واحدة | درجة وثقة متساويتان |
| غير مشكول | يحتمل قراءات كثيرة | قد يتعادل بحران، والثقة تنخفض ويُعلَن التعادل |

هذه خاصّية في الكتابة العربية لا عيب في الخوارزمية: نصّ من ١٤ وحدة بلا
تشكيل يحتمل قراءات مقاطعها بين ٧ و١٤.

المحرك **يُفصح** عن ذلك بدل أن يخفيه: حقل `ambiguity`، وفصل الدرجة عن
الثقة، وطلب التشكيل صراحةً عند التعادل.

---

## الحالة

| المرحلة | الحالة |
|---|---|
| PHASE 1 — نواة العروض | ✅ |
| PHASE 2 — قاعدة الأوزان | ✅ ١٢ مفعَّل + ١ معطَّل بوعي |
| PHASE 3 — المطابقة والدرجة | ✅ |
| PHASE 4 — الاختبارات | ✅ ٨٥ اختبارًا |
| PHASE 5 — تحليل القصيدة | ✅ |
| PHASE 6 — طبقة الخدمة | ✅ واجهة برمجية مستقلة عن العرض |
| PHASE 7 — تطبيق iOS | ⏳ آخر مرحلة، بعد أن يثبت المحرك نفسه |

**الناقص ليس كودًا بل بيانات تحقّق** — انظر
[`OPEN-QUESTIONS.md`](docs/OPEN-QUESTIONS.md).

---

## ملاحظة قانونية

لم يُنسخ أي كود أو خوارزمية أو ملف داخلي من أي تطبيق آخر. التنفيذ
مستقلّ بالكامل، مبنيّ على قواعد العروض العربي المعروفة والمادة
التعليمية المقدَّمة.
